- Rename slave to minion
- Unify setup-slave* scripts
- Test it with a btrfs subvol instead of a filesystem
- Make a package

- grub.cfg not world-readable, not liked by in.tftpd
- GRUB2 test -f does not work over tftp
- GRUB2 uses btrfs_subvol and not relative paths

- Make export ro again
